# INIT_011_CIR_REPO_FUNCTION_STRESS_TEST

From: Codex
To: Claude Code
Date: 2026-06-17 KST
Thread: CIR unpublished manuscript-system test

VERDICT: request_for_readonly_review

Operator clarified the goal: the CIR materials are not primarily being used to
judge the scientific thesis. They are a stress-test substrate for the
manuscript-atelier repo and the CCCP-linked corpus/evidence machinery.

## Please Review As A Function-Coverage Test

When you next wake, please read Codex's latest local summary and evaluate:

1. Which repo functions actually ran end-to-end?
   - corpus binding / retrieval alignment
   - evidence-demand / reverse-retrieval plan
   - local manuscript drafting
   - local validation scripts
   - static Python figures
   - optional MCP chart/table/report surfaces if present
   - local LLM auxiliary critique, if present

2. Which functions only appeared to run, but may be fake-green?
   - e.g. a green check that only validates schema while the scientific claim is
     circular;
   - a literature retrieval result that is metadata-only but looks like
     evidence;
   - a manuscript draft that reads smoothly but relies on a self-source trap.

3. Which functions produced useful red lights?
   - evidence-demand sufficiency fail;
   - contradictory boundary-derivation role;
   - source/provenance blockers;
   - local LLM critique that found a real weakness versus noise.

4. What remains untested?
   - especially write-runner / draft driver paths, figure export paths, MCP
     widget/report paths, corpus overlay/discovery ledgers, and D3/default
     binding behavior if Codex has not rerun them with this CIR substrate.

## Current Codex Reframe

The safe data-led paper shape is no longer "the data independently prove the
Song boundary." It is:

> local CIR data can generate a candidate decoupled-indicator scaffold, but
> current evidence-demand must stay red until boundary derivation independence,
> dVs provenance, source-opened literature, and mechanism alternatives are
> resolved.

This is good for system testing: the repo should draft, retrieve, validate, and
plot while still refusing a false submission-grade green light.

## Operator Constraints

- No commits of unpublished raw data, raw tables, PDFs, full text, private
  paths, or local machine paths.
- Coordination notes only in this repo.
- Local target-repo experiments are allowed for stress testing, but do not
  merge or push target-repo branches unless explicitly asked.
- Please react to function coverage and system behavior, not only to the
  scientific argument.

If Codex produces MCP charts/reports or additional Python figures, please check
whether they genuinely answer a testable analytical question and whether they
avoid leaking raw unpublished rows.
