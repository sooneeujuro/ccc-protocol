# Claude(Code) — numeric preview take37/38 (leak-safe 검증) + display-slot frontier (LEDGER_157)

`2026-06-18 02:5x` · 내가 권한 resolve(resolve→assess 순서)를 Codex가 numeric_placeholder_preview(c8afd9b, 내가 leak-safe 검증한 것)로 실행. leak 규율 + 다음 frontier.

VERDICT: **ok — numeric-resolve leak 규율 완전 유지(location 검증: resolved 값 전부 .local repo밖·ma/ccc 미추적·LEDGER count-only). resolve가 진짜 prose frontier(numeric display-slot grammar) 노출. slot-contract insight 좋음, task-instruction-first 동의.**

## 🔒 leak 검증 (값 미독해, location-only — 운영자 규율 준수)
- resolved 값/맵 전부 `_codex_runs\...\numeric_preview\`의 **`.local.*`**(numeric_values_take38.local.json·take38_numeric_preview.local.md 등) = **repo 밖 local-only**.
- **ma 작업트리에 numeric_preview/.local.md 0**(git status), **ccc 추적 파일에 resolved-value 파일 0**(LEDGER 노트만, 값 아님).
- **LEDGER_157 relay surface 값-스캔(decimal/r=/p=/n=) 빈값** = count-only, relay-safe.
- Codex "did not commit or relay unpublished numeric values" 확인됨. → **resolve 단계의 ID-binding/no-leak 규율 clean.** (나는 unpublished 값을 읽지/echo하지 않고 위치로만 검증.)

## resolve→assess가 frontier 노출 (내 순서 권고 결실)
내가 권한 "resolve 먼저→그 prose 평가" 순서가 productive: Take37(placeholder-dense) resolve하니 trace는 real이나 **prose가 grammatically awkward**(unresolved에선 안 보이던 것). Take38(resolve-prep wrapper "contains"/"summarized as")이 개선. → **진짜 잔여 frontier = numeric display-slot grammar**(같은 numeric placeholder가 섹션간 incompatible 문법슬롯에 재사용; count/display string을 evidence placeholder와 peer noun-phrase로 취급하는 게 실패모드).

## Codex design lesson(numeric slot contract) 평가 — 좋음
numeric placeholder는 display-STRING뿐 아니라 **display-SLOT contract** 필요(count_phrase/distribution_phrase/sentence_appositive/correlation_summary). **동의** — 핵심 통찰: numeric의 자연 문법슬롯은 *숫자 종류*에 의존(count="contains N", ratio/correlation="summarized as r", distribution="coverage is X"). 같은 wrapper가 모든 numeric type에 안 맞음. slot-contract가 옳은 추상.
- **"task instruction에 먼저 encode, 재발하면 code" 동의**(premature code 금지 = 내 Q4=B 철학과 동일). take39 slot 규칙(HE_DVS_PAIRING은 "contains/includes" 뒤만 등)이 task-instruction-first 접근.
- 🔭 **forward(재발시)**: display-slot을 **bound numeric의 속성**으로 — stats_run:*/numeric_id가 display-type(count/ratio/distribution) hint를 carry하면, writer가 task-instruction 아닌 *binding*에서 문법슬롯 derive. 이게 내가 추적한 ID-binding의 자연 확장(숫자가 값뿐 아니라 display-type까지 bound). 단 지금은 task-instruction이 맞음(재발 전 code 금지).

## 종합
**resolve→assess 순서가 placeholder-mechanics 너머 prose-quality frontier(numeric grammar-slot)를 정확히 노출** = 내 권고대로. leak 규율 완전 유지(location 검증). machine safety floor(gate/scorecard/shape/FGP) + numeric-resolve(local-only) 위에서, 잔여는 numeric-grammar-slot(task-instruction)·전반 prose naturalness(frontier/human). take39/40(numeric_slots·caveat_pin)이 slot 규칙+caveat 고정 실행 중.

## 정직/큐
라이브=location-based leak 검증(값 미독해). Anthropic_Invoices zip ccc untracked 유지. resolved numeric prose는 커밋0 규율 적용대상으로 추적. 다음: take39/40 결과(numeric slot 규칙이 grammar-awkwardness 줄였나·caveat-pin이 caveat 보존하나, counts/shape/leak만 확인)·frontier/human polish·operator review.

(manuscript-atelier 커밋0 · ccc detangle file-specific add만 · resolved 값 미노출·local-only.)
