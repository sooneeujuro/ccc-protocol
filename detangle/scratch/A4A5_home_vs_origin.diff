# geochem A4/A5 — origin/claude/p1-science-accuracy → 홈 로컬 (4dd92ecb) 직접 diff
# read-only 추출(커밋 0). '-'=origin 현재본 / '+'=홈본. Codex 리뷰용(inbox_codex/003).
# 홈 고유 verify 스크립트(origin 없음): scripts/verify_{correlation_null,export_sanitize,generator_sinks}.mjs
# ---
diff --git a/src/lib/python-export.ts b/src/lib/python-export.ts
index e77182ee..fddc8764 100644
--- a/src/lib/python-export.ts
+++ b/src/lib/python-export.ts
@@ -19,6 +19,7 @@
 
 import { ScatterPoint, PrecisionExportOptions } from './precision-export';
 import { JOURNAL_PRESETS, COLORBLIND_SAFE_PALETTE, getColumnWidthMm } from './journal-presets';
+import { safeColor, pyStr, safeLinestyle } from './export-sanitize';
 import { COMMUNITY_PRESETS } from './community-presets';
 
 // ─── Chemical Formula → matplotlib mathtext ──────────────────────────
@@ -40,21 +41,10 @@ import { COMMUNITY_PRESETS } from './community-presets';
  *   geochemistry labels like CH₄, SO₄, SiO₅ rendered as □ boxes.
  *   Phase B-2e reverted to mathtext for guaranteed font coverage.
  */
-/** Escape a string into a single-quoted Python literal (backslashes, quotes,
- *  newlines) so user-supplied label text cannot break out of the literal and
- *  inject code into the generated script. */
-function pyQuote(s: string): string {
-  return `'${s
-    .replace(/\\/g, '\\\\')
-    .replace(/'/g, "\\'")
-    .replace(/\n/g, '\\n')
-    .replace(/\r/g, '\\r')}'`;
-}
-
 function toMathtextLabel(label: string): string {
   const hasChemical = /(?:[A-Z][a-z]?\d|^\d+[A-Z]|[δd]\d+[A-Z])/.test(label);
   if (!hasChemical) {
-    return pyQuote(label);
+    return `'${label.replace(/\\/g, '\\\\').replace(/'/g, "\\'")}'`;
   }
   let result = label;
   // δ-notation: d18O / δ18O → $\mathit{\delta}^{18}$O
@@ -73,9 +63,10 @@ function toMathtextLabel(label: string): string {
   result = result.replace(/([A-Z][a-z]?)(\d+)/g, '$1$_{$2}$');
   // R/Ra → R/R$_a$
   result = result.replace(/R\/Ra\b/g, 'R/R$_a$');
-  // Non-raw escaped literal: doubled backslashes parse back to the same
-  // mathtext, and user quotes/newlines cannot terminate the literal.
-  return pyQuote(result);
+  // Normal (non-raw) string with backslashes doubled so matplotlib still receives
+  // literal mathtext backslashes, while a stray ' in the label cannot break out
+  // (a raw string r'...' could not escape an embedded quote).
+  return `'${result.replace(/\\/g, '\\\\').replace(/'/g, "\\'")}'`;
 }
 
 // --- Marker shape mapping ------------------------------------------------
@@ -107,7 +98,7 @@ export function generateMatplotlibCode(
   options: PrecisionExportOptions
 ): PythonExportResult {
   const allPresets = { ...JOURNAL_PRESETS, ...COMMUNITY_PRESETS };
-  const spec = options.presetOverride ?? allPresets[options.journal];
+  const spec = allPresets[options.journal];
   if (!spec) {
     throw new Error(`Unknown journal: ${options.journal}. Available: ${Object.keys(allPresets).join(', ')}`);
   }
@@ -117,15 +108,18 @@ export function generateMatplotlibCode(
   const font = spec.font;
   const line = spec.line;
 
-  const colors = (options.colorblindSafe !== false)
+  // §A5: user-supplied custom colors are sanitized at the source so every
+  // downstream `colors[i]` interpolation into generated Python is already safe.
+  const colors = ((options.colorblindSafe !== false)
     ? COLORBLIND_SAFE_PALETTE
-    : (options.colors || COLORBLIND_SAFE_PALETTE);
+    : (options.colors || COLORBLIND_SAFE_PALETTE)).map((c) => safeColor(c));
 
   const groups = Array.from(new Set(points.map(p => p.group || 'default')));
   const hasGroups = groups.length > 1 && groups[0] !== 'default';
   const hasErrorBars = points.some(p => p.xErr !== undefined || p.yErr !== undefined);
   const useShapes = (options.varyMarkerShapes !== false && hasGroups);
-  const tickDir = options.tickDirection || 'in';
+  const tickDir = ['in', 'out', 'inout'].includes(options.tickDirection as string)
+    ? (options.tickDirection as string) : 'in';
   // Marker clipping: default off (markers near axis edges remain whole).
   // Set clipMarkers=true to use matplotlib default clipping behavior.
   const clipArg = options.clipMarkers ? ', clip_on=True' : ', clip_on=False';
@@ -136,9 +130,7 @@ export function generateMatplotlibCode(
 
   w(`#!/usr/bin/env python3`);
   w(`"""`)
-  // spec.name can be user-controlled via custom presets — keep it from
-  // closing the docstring.
-  w(`Journal-compliant scatter plot — ${spec.name.replace(/\\/g, '\\\\').replace(/"/g, '\\"')}`);
+  w(`Journal-compliant scatter plot — ${spec.name}`);
   w(`Generated by GeoChemAnalyzer (sooneeujuro.com)`);
   w(`Figure: ${widthMm}×${heightMm}mm, ${options.columnWidth} column`);
   w(`"""`);
@@ -270,7 +262,7 @@ export function generateMatplotlibCode(
   w(`# Font settings — MUST be set BEFORE plt.subplots()!`);
   w(`# (Otherwise matplotlib uses DejaVu Sans instead of Arial)`);
   w(`plt.rcParams.update({`);
-  w(`    'font.family': '${font.family.split(',')[0].trim()}',`);
+  w(`    'font.family': ${pyStr(font.family.split(',')[0].trim())},`);
   w(`    'font.size': ${font.labelSize},`);
   w(`    'axes.labelsize': ${font.labelSize},`);
   w(`    'xtick.labelsize': ${font.labelSize},`);
@@ -303,7 +295,7 @@ export function generateMatplotlibCode(
       const g = groups[i];
       const shape = useShapes ? MARKER_SHAPES_ORDER[i % MARKER_SHAPES_ORDER.length] : 'circle';
       const marker = MATPLOTLIB_MARKERS[shape] || "'o'";
-      w(`    '${g}': {'color': '${colors[i % colors.length]}', 'marker': ${marker}},`);
+      w(`    ${pyStr(g)}: {'color': '${colors[i % colors.length]}', 'marker': ${marker}},`);
     }
     w(`}`);
   } else {
@@ -324,11 +316,11 @@ export function generateMatplotlibCode(
       // Only the first occurrence of a label gets the legend entry
       let labelArg = '';
       if (rl.label && !labelsSeen.has(rl.label)) {
-        labelArg = `, label='${rl.label}'`;
+        labelArg = `, label=${pyStr(rl.label)}`;
         labelsSeen.add(rl.label);
       }
       w(`ax.plot([${rl.x1}, ${rl.x2}], [${rl.y1}, ${rl.y2}],`);
-      w(`        color='${c}', linewidth=${lw}, linestyle=${ls}, alpha=0.5, zorder=2${labelArg})`);
+      w(`        color='${safeColor(c)}', linewidth=${lw}, linestyle=${ls}, alpha=0.5, zorder=2${labelArg})`);
     }
     w(``);
   }
@@ -358,7 +350,7 @@ export function generateMatplotlibCode(
       const mc = mixingCurves[mi];
       const A = mc.endmemberA;
       const B = mc.endmemberB;
-      const color = mc.color || '#E74C3C';
+      const color = safeColor(mc.color || '#E74C3C');
       const lw = mc.strokeWidth != null ? mc.strokeWidth : 1.5;
       const opacity = mc.opacity != null ? mc.opacity : 0.9;
       // Convert SVG dash-array (e.g. "5,3") to matplotlib linestyle tuple
@@ -467,11 +459,11 @@ export function generateMatplotlibCode(
       w(`ax.scatter(${B.x}, ${B.y}, marker='s', facecolors='${color}', edgecolors='black',`);
       w(`           linewidths=0.3, s=${markerSz}, zorder=4${clipArg})`);
       if (A.label && mc.showEndmemberLabels !== false) {
-        w(`ax.annotate('${A.label}', xy=(${A.x}, ${A.y}), xytext=(6, 4), textcoords='offset points',`);
+        w(`ax.annotate(${pyStr(A.label)}, xy=(${A.x}, ${A.y}), xytext=(6, 4), textcoords='offset points',`);
         w(`            fontsize=${font.labelSize * 0.8}, fontweight='bold', color='${color}')`);
       }
       if (B.label && mc.showEndmemberLabels !== false) {
-        w(`ax.annotate('${B.label}', xy=(${B.x}, ${B.y}), xytext=(6, 4), textcoords='offset points',`);
+        w(`ax.annotate(${pyStr(B.label)}, xy=(${B.x}, ${B.y}), xytext=(6, 4), textcoords='offset points',`);
         w(`            fontsize=${font.labelSize * 0.8}, fontweight='bold', color='${color}')`);
       }
       w(``);
@@ -505,7 +497,7 @@ export function generateMatplotlibCode(
         lsArg = `'-'`;
       }
       const lw = grid.strokeWidth;
-      const color = grid.color;
+      const color = safeColor(grid.color);
       const opacity = grid.opacity;
       const labelFontPt = (font.labelSize * 0.7).toFixed(1);
 
@@ -559,7 +551,7 @@ export function generateMatplotlibCode(
           // Slightly larger offset for fan so the label sits above the curve.
           const offset = grid.type === 'fan' ? '(4, 5)' : '(0, 3)';
           w(`ax.annotate('${safe}', xy=(${mx}, ${my}), xytext=${offset}, textcoords='offset points',`);
-          w(`            fontsize=${labelFontPt}, color='${color}', ha='center', zorder=1)`);
+          w(`            fontsize=${labelFontPt}, color='${safeColor(color)}', ha='center', zorder=1)`);
         }
       }
       w(``);
@@ -578,16 +570,16 @@ export function generateMatplotlibCode(
         w(`ax.errorbar(x_${varName}, y_${varName},`);
         if (hasYErr) w(`            yerr=y_err_${varName},`);
         if (hasXErr) w(`            xerr=x_err_${varName},`);
-        w(`            fmt=groups['${g}']['marker'], color=groups['${g}']['color'],`);
+        w(`            fmt=groups[${pyStr(g)}]['marker'], color=groups[${pyStr(g)}]['color'],`);
         w(`            markersize=${markerSizePt.toFixed(1)}, markeredgewidth=${(line.min * 0.5).toFixed(2)},`);
         w(`            capsize=${((options.errorBarCapMm || 0.5) * 2.835).toFixed(1)}, elinewidth=${(line.min * 0.75).toFixed(2)},`);
-        w(`            alpha=0.8, label='${g}', zorder=3${clipArg})`);
+        w(`            alpha=0.8, label=${pyStr(g)}, zorder=3${clipArg})`);
       } else {
         w(`ax.scatter(x_${varName}, y_${varName},`);
-        w(`           marker=groups['${g}']['marker'],`);
-        w(`           facecolors=groups['${g}']['color'], edgecolors='black', linewidths=0.3,`);
+        w(`           marker=groups[${pyStr(g)}]['marker'],`);
+        w(`           facecolors=groups[${pyStr(g)}]['color'], edgecolors='black', linewidths=0.3,`);
         w(`           s=${(markerSizePt * markerSizePt / 4).toFixed(0)}, alpha=0.8,`);
-        w(`           label='${g}', zorder=3${clipArg})`);
+        w(`           label=${pyStr(g)}, zorder=3${clipArg})`);
       }
       w(``);
     }
@@ -612,13 +604,13 @@ export function generateMatplotlibCode(
     w(`# --- Reference Points ---------------------------------------------`);
     const rpMarkerMap: Record<string, string> = { square: 's', circle: 'o', triangle: '^', diamond: 'D' };
     for (const rp of refPoints) {
-      const rpColor = rp.color || '#000';
+      const rpColor = safeColor(rp.color || '#000');
       const rpMarker = rpMarkerMap[rp.shape || 'square'] || 's';
       const rpSizePt = rp.size ? rp.size * 2.835 * 2 : markerSizePt;
       const rpS = (rpSizePt * rpSizePt / 4).toFixed(0);
       w(`ax.scatter(${rp.x}, ${rp.y},`);
       w(`           marker='${rpMarker}', facecolors='${rpColor}', edgecolors='black', linewidths=0.3,`);
-      w(`           s=${rpS}, zorder=4${rp.label ? `, label='${rp.label}'` : ''}${clipArg})`);
+      w(`           s=${rpS}, zorder=4${rp.label ? `, label=${pyStr(rp.label)}` : ''}${clipArg})`);
       if (rp.label) {
         const pos = rp.labelPosition || 'right';
         let xyOff: string;
@@ -630,7 +622,7 @@ export function generateMatplotlibCode(
           case 'left':   xyOff = '(-6, 0)'; ha = 'right';  va = 'center'; break;
           default:       xyOff = '(6, 0)';  ha = 'left';   va = 'center'; break;
         }
-        w(`ax.annotate('${rp.label}', xy=(${rp.x}, ${rp.y}),`);
+        w(`ax.annotate(${pyStr(rp.label)}, xy=(${rp.x}, ${rp.y}),`);
         w(`            xytext=${xyOff}, textcoords='offset points',`);
         w(`            fontsize=${font.labelSize * 0.85}, fontweight='bold', ha='${ha}', va='${va}')`);
       }
@@ -648,10 +640,10 @@ export function generateMatplotlibCode(
     w(`r_squared = r_value**2`);
     w(`x_line = np.linspace(ax.get_xlim()[0] if ax.get_xlim()[0] else ${xVar}.min(), `);
     w(`                      ax.get_xlim()[1] if ax.get_xlim()[1] else ${xVar}.max(), 100)`);
-    const trendColor = options.trendlineStyle?.color || '#CC0000';
+    const trendColor = safeColor(options.trendlineStyle?.color || '#CC0000');
     const trendWidth = options.trendlineStyle?.width ?? line.trend;
     const trendOpacity = options.trendlineStyle?.opacity ?? 0.7;
-    const trendDash = options.trendlineStyle?.dash || '--';
+    const trendDash = safeLinestyle(options.trendlineStyle?.dash);
     w(`ax.plot(x_line, slope * x_line + intercept,`);
     w(`        color='${trendColor}', linewidth=${trendWidth.toFixed(2)}, linestyle='${trendDash}', alpha=${trendOpacity}, zorder=2)`);
     w(``);
@@ -778,7 +770,7 @@ export function generateMatplotlibCode(
   // --- Panel label ---------------------------------------------------
   if (options.panelLabel) {
     w(`# Panel label`);
-    w(`ax.text(-0.12, 1.05, '${options.panelLabel}', transform=ax.transAxes,`);
+    w(`ax.text(-0.12, 1.05, ${pyStr(options.panelLabel)}, transform=ax.transAxes,`);
     w(`        fontsize=${font.panelLabelSize}, fontweight='bold', va='top')`);
     w(``);
   }
diff --git a/src/lib/statistics.ts b/src/lib/statistics.ts
index 1d3f6565..bfd8f380 100644
--- a/src/lib/statistics.ts
+++ b/src/lib/statistics.ts
@@ -329,12 +329,11 @@ export function calculateDescriptiveStats(data: number[]): AdvancedStatistics {
 }
 
 // 상관관계 매트릭스 계산
-// 계산 불가 셀(영분산, 길이 불일치 등)은 0("무상관")으로 둔갑시키지 않고
-// NaN으로 보존한다. JSON 직렬화 시 NaN → null이 되므로 클라이언트는
-// "측정된 r=0"과 "계산 불가"를 구분할 수 있다.
-export function calculateCorrelationMatrix(data: Record<string, number[]>): Record<string, Record<string, number>> {
+// 계산 불가 셀(분산 0 / 표본 부족 / 예외)은 null 로 반환한다.
+// r=0(측정된 무상관)과 "계산 불가"를 구분하기 위함 — 0 치환은 결측을 0으로 둔갑시키는 정책 위반.
+export function calculateCorrelationMatrix(data: Record<string, number[]>): Record<string, Record<string, number | null>> {
   const variables = Object.keys(data)
-  const matrix: Record<string, Record<string, number>> = {}
+  const matrix: Record<string, Record<string, number | null>> = {}
 
   variables.forEach(var1 => {
     matrix[var1] = {}
@@ -343,10 +342,10 @@ export function calculateCorrelationMatrix(data: Record<string, number[]>): Reco
         matrix[var1][var2] = 1
       } else {
         try {
-          // sampleCorrelation이 NaN을 반환하면 (예: zero-variance) 그대로 보존
-          matrix[var1][var2] = ss.sampleCorrelation(data[var1], data[var2])
+          const corr = ss.sampleCorrelation(data[var1], data[var2])
+          matrix[var1][var2] = Number.isFinite(corr) ? corr : null
         } catch {
-          matrix[var1][var2] = NaN
+          matrix[var1][var2] = null
         }
       }
     })
@@ -362,33 +361,32 @@ export function suggestPCAVariables(
   threshold: number = 0.6
 ): PCASuggestion[] {
   const suggestions: PCASuggestion[] = []
-
-  // 계산 불가(NaN) 상관 셀은 평균에서 제외한다 — 0(무상관)으로 취급 금지.
-  // 유한한 셀이 하나도 없으면 NaN을 반환해 호출부가 명시적으로 거른다.
-  const finiteAbsAvg = (values: number[]): number => {
-    const finite = values.filter(v => Number.isFinite(v))
-    if (finite.length === 0) return NaN
-    return finite.reduce((sum, v) => sum + Math.abs(v), 0) / finite.length
-  }
-
+  
   // 상관관계가 높은 변수 그룹 찾기
   const groups: string[][] = []
   const used = new Set<string>()
-
+  
   for (let i = 0; i < variableNames.length; i++) {
     if (used.has(variableNames[i])) continue
-
+    
     const group = [variableNames[i]]
     used.add(variableNames[i])
-
+    
     for (let j = i + 1; j < variableNames.length; j++) {
       if (used.has(variableNames[j])) continue
-
-      // 그룹 내 모든 변수와의 평균 상관계수 계산 (계산 불가 셀 제외)
-      const avgCorr = finiteAbsAvg(
-        group.map(groupVar => correlationMatrix[variableNames.indexOf(groupVar)][j])
-      )
-      if (Number.isFinite(avgCorr) && avgCorr >= threshold) {
+      
+      // 그룹 내 모든 변수와의 평균 상관계수 계산
+      let totalCorr = 0
+      let count = 0
+      
+      for (const groupVar of group) {
+        const groupIdx = variableNames.indexOf(groupVar)
+        totalCorr += Math.abs(correlationMatrix[groupIdx][j])
+        count++
+      }
+      
+      const avgCorr = totalCorr / count
+      if (avgCorr >= threshold) {
         group.push(variableNames[j])
         used.add(variableNames[j])
       }
@@ -405,36 +403,35 @@ export function suggestPCAVariables(
     const sortedByVariance = group.sort((a, b) => {
       const aIdx = variableNames.indexOf(a)
       const bIdx = variableNames.indexOf(b)
-
-      // 다른 변수들과의 평균 상관계수로 정렬 (높은 순, 계산 불가 셀 제외)
-      const inGroupCells = (selfIdx: number): number[] =>
-        correlationMatrix[selfIdx].filter((_, idx) =>
-          idx !== selfIdx && group.includes(variableNames[idx])
-        )
-      const aAvgCorr = finiteAbsAvg(inGroupCells(aIdx))
-      const bAvgCorr = finiteAbsAvg(inGroupCells(bIdx))
-
-      // 평균이 계산 불가인 변수는 뒤로 보낸다
-      if (!Number.isFinite(aAvgCorr) && !Number.isFinite(bAvgCorr)) return 0
-      if (!Number.isFinite(aAvgCorr)) return 1
-      if (!Number.isFinite(bAvgCorr)) return -1
+      
+      // 다른 변수들과의 평균 상관계수로 정렬 (높은 순)
+      const aAvgCorr = correlationMatrix[aIdx].reduce((sum, corr, idx) => {
+        if (idx !== aIdx && group.includes(variableNames[idx])) {
+          return sum + Math.abs(corr)
+        }
+        return sum
+      }, 0) / (group.length - 1)
+      
+      const bAvgCorr = correlationMatrix[bIdx].reduce((sum, corr, idx) => {
+        if (idx !== bIdx && group.includes(variableNames[idx])) {
+          return sum + Math.abs(corr)
+        }
+        return sum
+      }, 0) / (group.length - 1)
+      
       return bAvgCorr - aAvgCorr
     })
-
+    
     const selectedVariables = sortedByVariance.slice(0, 6) // 최대 6개
-
-    // 가상 PCA 검증: 최소 2개 컴포넌트가 의미있는지 확인 (계산 불가 셀 제외)
-    const pairCells: number[] = []
-    for (let i = 0; i < selectedVariables.length; i++) {
-      for (let j = i + 1; j < selectedVariables.length; j++) {
-        const idx1 = variableNames.indexOf(selectedVariables[i])
-        const idx2 = variableNames.indexOf(selectedVariables[j])
-        pairCells.push(correlationMatrix[idx1][idx2])
-      }
-    }
-    const avgCorrelation = finiteAbsAvg(pairCells)
-    // 모든 쌍이 계산 불가면 이 그룹은 추천할 근거가 없다
-    if (!Number.isFinite(avgCorrelation)) return
+    
+    // 가상 PCA 검증: 최소 2개 컴포넌트가 의미있는지 확인
+    const avgCorrelation = selectedVariables.reduce((sum, var1, i) => {
+      return sum + selectedVariables.slice(i + 1).reduce((innerSum, var2) => {
+        const idx1 = variableNames.indexOf(var1)
+        const idx2 = variableNames.indexOf(var2)
+        return innerSum + Math.abs(correlationMatrix[idx1][idx2])
+      }, 0)
+    }, 0) / (selectedVariables.length * (selectedVariables.length - 1) / 2)
     
     // 상관관계 강도에 따른 예상 분산 설명력 추정 (개선된 공식)
     const estimatedPC1Variance = Math.min(avgCorrelation * 50 + 40, 80) // 40-80% 범위
diff --git a/src/lib/ternary-piper-export.ts b/src/lib/ternary-piper-export.ts
index 046d5d15..702fc8ba 100644
--- a/src/lib/ternary-piper-export.ts
+++ b/src/lib/ternary-piper-export.ts
@@ -9,6 +9,8 @@
  * Journal-quality output with preset styling.
  */
 
+import { safeColor, pyStr, pyDocSafe } from './export-sanitize'
+
 export interface TernaryData {
   label: string
   a: number  // top vertex component
@@ -84,51 +86,6 @@ const EQ_WEIGHT = {
   Cl:  35.45,   // 35.45 / 1
 }
 
-/** Ion fields needed for the meq conversion (structural subset of PiperData). */
-export interface PiperIonValues {
-  Ca: number; Mg: number; Na_K: number; K?: number;
-  Cl: number; SO4: number; HCO3_CO3: number;
-}
-
-/**
- * Convert one Piper sample to meq-% (sum=100 per ion group).
- * Piper diagrams REQUIRE meq/L (charge-equivalent concentrations) for correct
- * chemistry. Exported as the single conversion shared by the Python export
- * path AND the SVG preview path (svg-piper.ts) so the two cannot drift.
- */
-export function piperToMeqPercent(
-  p: PiperIonValues,
-  inputUnit: 'mg_L' | 'meq_L' | 'percent' = 'mg_L',
-): { ca: number; mg: number; nak: number; cl: number; so4: number; hco3: number } {
-  let ca_eq: number, mg_eq: number, nak_eq: number
-  let cl_eq: number, so4_eq: number, hco3_eq: number
-  if (inputUnit === 'mg_L') {
-    // Divide mg/L by equivalent weight to get meq/L
-    ca_eq = p.Ca / EQ_WEIGHT.Ca
-    mg_eq = p.Mg / EQ_WEIGHT.Mg
-    // If K provided separately, Na_K is Na alone → use each equivalent weight
-    // Otherwise Na_K is combined → use Na equiv weight (approximation for Na-dominant waters, <5% error)
-    if (p.K !== undefined && p.K > 0) {
-      nak_eq = p.Na_K / EQ_WEIGHT.Na + p.K / EQ_WEIGHT.K
-    } else {
-      nak_eq = p.Na_K / EQ_WEIGHT.Na
-    }
-    cl_eq   = p.Cl / EQ_WEIGHT.Cl
-    so4_eq  = p.SO4 / EQ_WEIGHT.SO4
-    hco3_eq = p.HCO3_CO3 / EQ_WEIGHT.HCO3
-  } else {
-    // 'meq_L' or 'percent': use values directly (just normalize)
-    ca_eq = p.Ca; mg_eq = p.Mg; nak_eq = p.Na_K
-    cl_eq = p.Cl; so4_eq = p.SO4; hco3_eq = p.HCO3_CO3
-  }
-  const cT = ca_eq + mg_eq + nak_eq || 1
-  const aT = cl_eq + so4_eq + hco3_eq || 1
-  return {
-    ca: ca_eq/cT*100, mg: mg_eq/cT*100, nak: nak_eq/cT*100,
-    cl: cl_eq/aT*100, so4: so4_eq/aT*100, hco3: hco3_eq/aT*100,
-  }
-}
-
 const JOURNAL_DEFAULTS: Record<string, { dpi: number; fontFamily: string; fontSize: number; figWidth: number }> = {
   nature:  { dpi: 1000, fontFamily: 'Arial',           fontSize: 7,  figWidth: 89 },
   chemgeo: { dpi: 600,  fontFamily: 'Arial',           fontSize: 8,  figWidth: 90 },
@@ -148,7 +105,7 @@ export function generateTernaryPython(config: TernaryConfig): string {
   const font = config.fontFamily || preset.fontFamily
   const fs = config.fontSize || preset.fontSize
   const ms = config.markerSize || 40
-  const colors = config.colorPalette || DEFAULT_COLORS
+  const colors = (config.colorPalette || DEFAULT_COLORS).map((c) => safeColor(c))
   const gridInt = config.gridInterval || 0.2
 
   // Group data
@@ -163,9 +120,9 @@ export function generateTernaryPython(config: TernaryConfig): string {
 
   let py = `#!/usr/bin/env python3
 """
-Ternary Diagram: ${config.title || 'Ternary Plot'}
+Ternary Diagram: ${pyDocSafe(config.title || 'Ternary Plot')}
 Generated by ClaudeOrchestra / geochemistry-analyzer
-Journal preset: ${config.journalPreset || 'chemgeo'}
+Journal preset: ${pyDocSafe(config.journalPreset || 'chemgeo')}
 
 Requirements: pip install python-ternary matplotlib numpy
 """
@@ -174,7 +131,7 @@ import matplotlib.pyplot as plt
 import matplotlib
 import numpy as np
 
-matplotlib.rcParams['font.family'] = '${font}'
+matplotlib.rcParams['font.family'] = ${pyStr(font)}
 matplotlib.rcParams['font.size'] = ${fs}
 matplotlib.rcParams['axes.linewidth'] = 0.8
 matplotlib.rcParams['pdf.fonttype'] = 42  # TrueType for Illustrator
@@ -191,9 +148,9 @@ tax.gridlines(multiple=${gridInt * 100}, color='#CCCCCC', linewidth=0.3)
 ${config.showGrid !== false ? "tax.ticks(axis='lbr', multiple=20, linewidth=0.5, tick_formats='%d', offset=0.02)" : '# ticks disabled'}
 
 # Apex labels
-tax.top_corner_label('${topLabel}', fontsize=${fs + 1}, offset=0.14)
-tax.left_corner_label('${blLabel}', fontsize=${fs + 1}, offset=0.14)
-tax.right_corner_label('${brLabel}', fontsize=${fs + 1}, offset=0.14)
+tax.top_corner_label(${pyStr(topLabel)}, fontsize=${fs + 1}, offset=0.14)
+tax.left_corner_label(${pyStr(blLabel)}, fontsize=${fs + 1}, offset=0.14)
+tax.right_corner_label(${pyStr(brLabel)}, fontsize=${fs + 1}, offset=0.14)
 
 # --- Color palette ---
 colors = ${JSON.stringify(colors)}
@@ -207,8 +164,8 @@ markers = ['o', 's', '^', 'D', 'v', 'P', 'X', 'h']
     for (const region of config.regions) {
       const verts = region.vertices.map(v => `(${v[0] * 100}, ${v[1] * 100}, ${v[2] * 100})`)
       py += `region_${region.label.replace(/\W/g, '_')} = [${verts.join(', ')}]\n`
-      py += `tax.polygon(region_${region.label.replace(/\W/g, '_')}, color='${region.color}', alpha=${region.alpha})\n`
-      py += `# Label: ${region.label}\n\n`
+      py += `tax.polygon(region_${region.label.replace(/\W/g, '_')}, color='${safeColor(region.color)}', alpha=${region.alpha})\n`
+      py += `# Label: ${pyDocSafe(region.label)}\n\n`
     }
   }
 
@@ -220,20 +177,20 @@ markers = ['o', 's', '^', 'D', 'v', 'P', 'X', 'h']
       return `(${(p.a / total * 100).toFixed(2)}, ${(p.b / total * 100).toFixed(2)}, ${(p.c / total * 100).toFixed(2)})`
     }).join(',\n    ')
 
-    py += `# Group: ${groupName}
+    py += `# Group: ${pyDocSafe(groupName)}
 pts_${groupIdx} = [
     ${ptsStr}
 ]
 tax.scatter(pts_${groupIdx}, marker=markers[${groupIdx} % len(markers)],
             color=colors[${groupIdx} % len(colors)], s=${ms},
-            label='${groupName}', zorder=5, edgecolors='black', linewidths=0.3)
+            label=${pyStr(groupName)}, zorder=5, edgecolors='black', linewidths=0.3)
 
 `
     groupIdx++
   }
 
   py += `# --- Legend & Title ---
-${config.title ? `tax.set_title('${config.title}', fontsize=${fs + 2}, pad=15)` : ''}
+${config.title ? `tax.set_title(${pyStr(config.title)}, fontsize=${fs + 2}, pad=15)` : ''}
 tax.legend(loc='upper right', fontsize=${fs - 1}, framealpha=0.8)
 tax.clear_matplotlib_ticks()
 tax.get_axes().axis('off')
@@ -256,7 +213,7 @@ export function generatePiperPython(config: PiperConfig): string {
   const font = config.fontFamily || preset.fontFamily
   const fs = config.fontSize || preset.fontSize
   const ms = config.markerSize || 40
-  const colors = config.colorPalette || DEFAULT_COLORS
+  const colors = (config.colorPalette || DEFAULT_COLORS).map((c) => safeColor(c))
   const showTypes = config.showWaterTypes !== false
 
   // Group data
@@ -268,7 +225,38 @@ export function generatePiperPython(config: PiperConfig): string {
   }
 
   const inputUnit = config.inputUnit || 'mg_L'
-  const toMeqPercent = (p: PiperData) => piperToMeqPercent(p, inputUnit)
+
+  // Convert input → meq/L → % (sum=100 per ion group)
+  // Piper diagrams REQUIRE meq/L (milliequivalents/L = charge-equivalent concentrations) for correct chemistry.
+  const toMeqPercent = (p: PiperData) => {
+    let ca_eq: number, mg_eq: number, nak_eq: number
+    let cl_eq: number, so4_eq: number, hco3_eq: number
+    if (inputUnit === 'mg_L') {
+      // Divide mg/L by equivalent weight to get meq/L
+      ca_eq = p.Ca / EQ_WEIGHT.Ca
+      mg_eq = p.Mg / EQ_WEIGHT.Mg
+      // If K provided separately, Na_K is Na alone → use each equivalent weight
+      // Otherwise Na_K is combined → use Na equiv weight (approximation for Na-dominant waters, <5% error)
+      if (p.K !== undefined && p.K > 0) {
+        nak_eq = p.Na_K / EQ_WEIGHT.Na + p.K / EQ_WEIGHT.K
+      } else {
+        nak_eq = p.Na_K / EQ_WEIGHT.Na
+      }
+      cl_eq   = p.Cl / EQ_WEIGHT.Cl
+      so4_eq  = p.SO4 / EQ_WEIGHT.SO4
+      hco3_eq = p.HCO3_CO3 / EQ_WEIGHT.HCO3
+    } else {
+      // 'meq_L' or 'percent': use values directly (just normalize)
+      ca_eq = p.Ca; mg_eq = p.Mg; nak_eq = p.Na_K
+      cl_eq = p.Cl; so4_eq = p.SO4; hco3_eq = p.HCO3_CO3
+    }
+    const cT = ca_eq + mg_eq + nak_eq || 1
+    const aT = cl_eq + so4_eq + hco3_eq || 1
+    return {
+      ca: ca_eq/cT*100, mg: mg_eq/cT*100, nak: nak_eq/cT*100,
+      cl: cl_eq/aT*100, so4: so4_eq/aT*100, hco3: hco3_eq/aT*100,
+    }
+  }
 
   // Build data arrays for each group (in Python syntax)
   let groupsCode = ''
@@ -284,7 +272,7 @@ export function generatePiperPython(config: PiperConfig): string {
     const catArr = catPts.map(x => `[${x[0].toFixed(3)}, ${x[1].toFixed(3)}, ${x[2].toFixed(3)}]`).join(', ')
     const anArr = anPts.map(x => `[${x[0].toFixed(3)}, ${x[1].toFixed(3)}, ${x[2].toFixed(3)}]`).join(', ')
     groupsCode += `
-# Group ${gi}: ${groupName}
+# Group ${gi}: ${pyDocSafe(groupName)}
 groups_data.append({
     'name': ${JSON.stringify(groupName)},
     'color': colors[${gi} % len(colors)],
@@ -303,9 +291,9 @@ groups_data.append({
 
   const py = `#!/usr/bin/env python3
 """
-Piper Diagram: ${config.title || 'Piper Plot'}
+Piper Diagram: ${pyDocSafe(config.title || 'Piper Plot')}
 Generated by geochemistry-analyzer (sooneeujuro.com)
-Journal preset: ${config.journalPreset || 'chemgeo'}
+Journal preset: ${pyDocSafe(config.journalPreset || 'chemgeo')}
 
 Math spec (Piper 1944) — s=1, g=0.3:
   Cation △: VCa=(0,0), VNaK=(1,0), VMg=(0.5, √3/2)
@@ -325,12 +313,12 @@ import matplotlib.pyplot as plt
 import numpy as np
 
 # ===== Font setup (BEFORE subplots, required for CodeDrop) =====
-plt.rcParams['font.family'] = '${font}'
+plt.rcParams['font.family'] = ${pyStr(font)}
 plt.rcParams['font.size'] = ${fs}
 plt.rcParams['axes.linewidth'] = 0.5
 plt.rcParams['mathtext.fontset'] = 'custom'
-plt.rcParams['mathtext.rm'] = '${font}'
-plt.rcParams['mathtext.it'] = '${font}:italic'
+plt.rcParams['mathtext.rm'] = ${pyStr(font)}
+plt.rcParams['mathtext.it'] = ${pyStr(font + ':italic')}
 plt.rcParams['svg.fonttype'] = 'none'
 plt.rcParams['pdf.fonttype'] = 42
 
