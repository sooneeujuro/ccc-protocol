# LEDGER_307_CODEX_TOURNAMENT_OUTPUT_SELF_CHECK

VERDICT: ok

Scope:
- Continued Gemma tournament harness hardening after LEDGER_306.
- No model calls.
- No manuscript/corpus data push.
- No raw model prose, protected source text, or resolved numeric values relayed here.

Change:
- Added runner-side output manifest consistency checks before writing run/scoring manifests.
- The check verifies:
  - run/scoring schemas
  - matching tournament/timestamp fields
  - compact UTC timestamp shape
  - local/provenance bits
  - entry_count / passed_count / failed_count match the actual entries
  - run and scoring entry id sets match exactly
  - persona/repetition/status/attempt/error/timing metadata match per blind id
  - passed scoring rows have safe response file paths and SHA-256 hashes
  - failed scoring rows have no response file or response hash
  - scoring rows do not carry prompt-pack paths, attempts, or gate summaries
- Added tests for:
  - response-only scoring manifests that include failed rows safely
  - self-check rejection when scoring counts drift

Why:
- The scoring manifest intentionally remains response-only but includes all blind ids, including failed rows.
- Before this patch, run/scoring outputs were generated by adjacent code paths but not cross-checked as a pair before disk write.
- This hardens the output side so summary counts and per-entry status cannot silently diverge.

Verification:
- `python -m pytest tools\paper-orchestra\local-llm\v0\tests\test_gemma_tournament_runner_synthetic.py -q`
  - 35 passed
- `python -m pytest tools\paper-orchestra\local-llm\v0\tests -q`
  - 247 passed
- `python -m pytest tools\paper-orchestra\writing-runner\v0\tests -q`
  - 468 passed
- `git diff --check -- tools\paper-orchestra\local-llm\v0 tools\paper-orchestra\writing-runner\v0`
  - no whitespace errors
  - only existing CRLF normalization warnings

Notes:
- `manuscript-atelier` remains uncommitted by design.
- ccc-protocol unrelated untracked files, including invoice and detangle JSON files, were not touched.
